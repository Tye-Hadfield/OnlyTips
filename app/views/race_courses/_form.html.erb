<%= form_with(model: race_course, local: true) do |form| %>
  <% if race_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(race_course.errors.count, "error") %> prohibited this race_course from being saved:</h2>

      <ul>
        <% race_course.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :race_course_name %>
    <%= form.text_field :race_course_name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
